Use Git, Node.js 20 or newer, and a compatible Pi installation. Clone the repository and open it in Pi; trust the project so Pi can load the generated project-local workflow resources. No dependency installation, build step, service, or environment variable is currently required.

GitHub SSH access is required only to clone the private remote or install it as a Git package. Run `/reload` after changing a resource during an active Pi session.
